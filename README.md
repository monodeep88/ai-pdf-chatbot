# PDF Chatbot Assistant

Type: **RAG**

PDF Chatbot is a complete, runnable Python starter project generated by AI Project Generator. It is intentionally dependency-light so it can run anywhere, but it includes real application code rather than only folders.

## Included

- Agent behavior tailored for `RAG`
- Local knowledge base in `data/knowledge_base.json`
- Retrieval, planning, multi-agent, or tool-routing logic depending on the project type
- Command line interface
- Automated pytest coverage
- Clear extension points for adding a real LLM, vector database, web UI, or API

## Run locally

```bash
pip install -r requirements.txt
pytest
python -m pdf_chatbot.cli "help me solve a real user problem"
```

## How to customize

- Add domain documents to `pdf_chatbot/data/knowledge_base.json`
- Extend `pdf_chatbot/agents.py` with API calls or richer workflows
- Add more tools in `pdf_chatbot/tools.py`
- Add tests for every new behavior in `tests/`
